Here you can get the database connection object.
Also here is the initial setup of the SQLite3 database. An production database, such as postgres, must be configured separately when starting the container.
